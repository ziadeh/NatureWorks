'use strict';

/**
 * startup-assessment router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::startup-assessment.startup-assessment');
